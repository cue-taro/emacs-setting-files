;; 環境変数の設定
(require 'init-setenv)

;; 色、フォントの設定
(require 'init-font-color-theme)

;; shell の設定
(require 'init-shell)

(require 'init-php)

(require 'init-javascript)

(require 'init-auto-complete)

(require 'init-mmm)




(provide 'init-main)